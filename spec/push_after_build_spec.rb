require 'spec_helper'

describe PushAfterBuild do
  it 'has a version number' do
    expect(PushAfterBuild::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
